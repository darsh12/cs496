{% macro charDecks(charDeck) %}
    <div id="charDeck">
        <h4 class="header-center">Character Decks</h4>
        {% for charDecks in charDeck %}

            <div class="card js-deck-item" data-id="{{ charDecks.id }}" onclick="selectCharDeck(this)"
                 style="margin-top: 7%">
                <div class="card-header text-info text-center">
                    <h3> {{ charDecks.name }}</h3>
                </div>
                <div class="card-body">
                    <div class="card-title text-center">
                        <button class="btn btn-primary" type="button" data-toggle="collapse"
                                data-target="#{{ charDecks.id }}" aria-expanded="false"
                                aria-controls="{{ charDecks.id }}">
                            Show Cards
                        </button>
                    </div>
                    <div class="collapse" id="{{ charDecks.id }}">
                        <blockquote class="blockquote text-muted">
                            <ul class="list-group list-group-flush">

                                <li data-value="{{ charDecks.card1.id }}" onclick="showCardPopup(this, 'char')"
                                    class="list-group-item popup-item">{{ charDecks.card1.charCard.charName }}
                                    {#- {{ charDecks.card1.charCard.charTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ charDecks.card2.id }}" onclick="showCardPopup(this, 'char')"
                                    class="list-group-item popup-item">{{ charDecks.card2.charCard.charName }}
                                    {#- {{ charDecks.card2.charCard.charTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ charDecks.card3.id }}" onclick="showCardPopup(this, 'char')"
                                    class="list-group-item popup-item">{{ charDecks.card3.charCard.charName }}
                                    {#- {{ charDecks.card3.charCard.charTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ charDecks.card4.id }}" onclick="showCardPopup(this, 'char')"
                                    class="list-group-item popup-item">{{ charDecks.card4.charCard.charName }}
                                    {#- {{ charDecks.card4.charCard.charTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ charDecks.card5.id }}" onclick="showCardPopup(this, 'char')"
                                    class="list-group-item popup-item">{{ charDecks.card5.charCard.charName }}
                                    {#- {{ charDecks.card5.charCard.charTier }}#}
                            </ul>                                    <i class="fas fa-external-link-alt"></i></li>

                        </blockquote>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endmacro %}

{% macro utilDecks(utilDeck) %}
    <div id="utilDeck">
        <h4 class="header-center">Utility Decks</h4>
        {% for utilDecks in utilDeck %}

            <div class="card js-deck-item" style="background-color: #FFC200; margin-top: 7%"
                 data-id="{{ utilDecks.id }}" onclick="selectUtilDeck(this)">
                <div class="card-header text-info text-center" style="background-color: blue">
                    <h3 class="" style="color: #FFFFFF">{{ utilDecks.name }}</h3>
                </div>
                <div class="card-body">
                    <div class="card-title text-center">
                        <button class="btn btn-primary" data-toggle="collapse" data-target="#{{ utilDecks.id }}"
                                aria-expanded="false" aria-controls="{{ utilDecks.id }}">
                            Show Cards
                        </button>
                    </div>
                    <div class="collapse" id="{{ utilDecks.id }}">

                        <blockquote class="blockquote text-muted">
                            <ul class="list-group list-group-flush">

                                <li data-value="{{ utilDecks.card1.id }}" onclick="showCardPopup(this, 'util')"
                                    class="list-group-item popup-item">{{ utilDecks.card1.utilCard.utilName }}
                                    {#- {{ utilDecks.card1.utilCard.utilTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ utilDecks.card2.id }}" onclick="showCardPopup(this, 'util')"
                                    class="list-group-item popup-item">{{ utilDecks.card2.utilCard.utilName }}
                                    {#- {{ utilDecks.card2.utilCard.utilTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>

                                <li data-value="{{ utilDecks.card3.id }}" onclick="showCardPopup(this, 'util')"
                                    class="list-group-item popup-item">{{ utilDecks.card3.utilCard.utilName }}
                                    {#- {{ utilDecks.card3.utilCard.utilTier }}#}
                                    <i class="fas fa-external-link-alt"></i></li>
                            </ul>

                        </blockquote>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endmacro %}